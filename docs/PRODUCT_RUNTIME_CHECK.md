# Product runtime check

実機で確認した操作と観察結果を記録する。自動化ツールの要素IDは画面更新で変わるため、操作前にアクセシビリティツリーを再取得する。

## 2026-09-06 macOS

- 最新の `MultiContext.app` が起動し、`Navier-Stokes Demo` ワークスペースを表示できた。
- AIスタックは「準備完了」と表示された。
- 4チャット、待機キュー1件、要対応状態、統合レポート表示を確認した。
- ブロック中のチャットに「履歴を整理」「整理後に再試行」が表示され、原因と復旧手順が読める状態だった。
- 新規ワークスペースダイアログを開くと、名前欄と文字数 `0 / 120` が表示された。
- 空入力で作成を実行すると、必須入力の検証が表示された。
- `Escape` で新規ワークスペースダイアログを閉じられた。
- 自動操作では動的なアクセシビリティ要素IDのずれにより、キャンセル操作として作成側の検証が発火する事象があった。操作前のツリー再取得が必要。
- 長大履歴向け描画改善後の最新ビルドを再起動し、画面表示が崩れないことを確認した。履歴は画面外描画の遅延対象として扱われ、アクセシビリティツリーの過剰な展開も緩和された。
- ダークモードへ切り替え、ワークスペース、ブロックバナー、概要カード、キュー表示のレイアウトが維持されることを確認した。ライトモードへ戻した後も状態は保持された。
- メイン画面からデスクトップ設定を開き、設定値を変更せずに「閉じる」を実行した。メイン画面へ戻った後もワークスペース、キュー、ブロック状態が維持された。
- ショートカットと使い方のヘルプを開き、内容が表示されること、閉じた後にメイン画面とワークスペース状態へ戻れることを確認した。
- ブロック中の `B — Fluid Dynamics` を再確認し、履歴保持・原因説明・キュー1件・「履歴を整理」「整理後に再試行」の復旧導線が同時に表示されることを確認した。履歴整理はデータ変更を伴うため、実行せず表示確認で終了した。
- 実機操作では画面外にあるチャット内ボタンのアクセシビリティ要素IDが無効になる場合があり、動的IDだけに依存する自動操作は不安定。製品UIの不具合とは分離して記録する。
- `feda3f1` の本番ビルドを再起動し、AIスタック準備完了、既存ワークスペース、4チャット、キュー1件、ブロック状態が維持されることを確認した。
- `eec7349` の本番ビルドを再起動し、チャットカードの操作ボタン、4チャットの履歴、ブロック中の復旧ボタンが正常に描画されることを確認した。
- 本番アプリのローカルAPIから待機中の `A — PDE Analyst` へ疎通プロンプトを送信した。キュー投入後、約12秒以内にLibreChat/gpt-ossから `READY` が返り、対象チャットは `idle`・キュー0・エラーなしへ復帰した。既存の `B — Fluid Dynamics` のブロック状態は維持された。
- 待機中の `C — Scaling Analyst` と `D — Reviewer` へほぼ同時に疎通プロンプトを投入した。両方が同時に `running` へ遷移し、約30秒以内にそれぞれ `C_READY` / `D_READY` を受信、両チャットとも `idle`・キュー0・エラーなしへ復帰した。Bの既存ブロック状態は維持された。
- Aチャットの連携エンドポイントからCチャットを一覧取得し、`C_READY` を検索した。対象チャット名、メッセージID、時刻、role、内容が返り、クロスチャット参照の実経路が動作した。履歴変更や秘密情報の露出はなかった。
- Aチャットの連携エンドポイントからCチャットへクロスチャット送信を行った。配送が受理され、C側で `CROSS_READY` が返り、キュー0・`idle`・エラーなしへ復帰した。冪等キーも付与して実行した。
- 同じ冪等キー・同じ内容でクロスチャット送信を再送した。`replayed: true` と初回と同じキューIDが返り、C側の `CROSS_READY` は1件のまま、キュー0・エラーなしだった。重複配信は発生しなかった。
- インシデント: 削除APIのガード確認時、確認時点でキューが消化済みになっており、対象ワークスペースが削除された。UIの保護表示だけでなく、削除前に最新状態を再確認する必要がある。バックアップ・一時ファイル・ログから完全な復元データは発見できなかった。今後は破壊的操作の実機検証に本番データを使わず、専用ワークスペースで行う。
- インシデント対策後、最新ビルドのサーバーを再起動し専用一時ワークスペースで削除APIを確認した。確認名なしは `DELETE_CONFIRMATION_REQUIRED` で拒否され、正確な `confirm_name` 付きのみ204で削除された。既存ワークスペースは変更しなかった。
- 専用スモークワークスペースを作成し、2チャットへ同一Agentを設定して一斉送信した。2チャットが実行され、両方から `BROADCAST_READY` が返り、最終状態 `SETTLED`・キュー0・エラーなしとなった。確認名付き削除で専用ワークスペースを後片付けした。
- 専用スモークワークスペースで2チャットに `COMPILE_SOURCE_READY` を生成させ、`SETTLED` 到達後にCompileを実行した。統合レポートが生成され、Compile前後で各チャットの履歴件数は2件のまま変化しなかった。確認名付き削除で後片付けした。
- 専用ワークスペースで実行中にCompileを呼び出し、`WORKSPACE_NOT_SETTLED` で拒否されることを確認した。停止直後のstopレスポンスは一時的に `RUNNING` と表示されたが、削除時には停止済みとして処理された。停止直後の表示反映遅延は残課題として扱う。
- 停止待機改善後、専用ワークスペースで送信直後に停止した。stopレスポンスは `SETTLED`、メンバーは `idle`、キュー0・実行中なしとなり、停止直後の状態表示遅延が解消された。専用環境は削除済み。
- 状態ファイルのバックアップ復元を一時ディレクトリで確認した。2回保存後に主ファイルを破損させて再読込すると、直前の `.bak` からワークスペースが復元され、`backup-restore=ok` となった。製品データは変更していない。

## 2026-09-07 macOS

- 画面更新警告を再現し、診断欄から `Can't find variable: compileHasSource` を特定した。Compileボタンの可否判定が `refresh()` 内のローカル変数を `wire()` から参照していたため、描画後のイベント接続が毎回失敗していた。
- `wire()` 内で保存済みメンバー履歴から回答有無を再計算するよう修正し、本番ビルド・アプリ再起動後に警告が消え、実行管理バー、Broadcast、Compile、チャット操作が表示されることを確認した。APIヘルスは HTTP 200、AIスタックは準備完了。
- Agent一覧の取得失敗とAgent 0件をUI上で分離し、取得失敗時にAgent作成を誤案内しない変更を本番ビルドへ反映した。再起動後、Agent 2件、AIスタック準備完了、ワークスペース更新警告なしを確認した。
- `SETTLED` の表示を `処理完了` に改め、内容の正しさや合意を意味しないランタイム状態であることを明確化した。アイドル時の実行管理バーでは一時停止を無効表示した。
- 新規ワークスペース作成で最初のチャット数を1〜4件から選べるようにした。既存のREST/MCP作成経路を壊さず、macOS本番ビルドとAPIヘルス HTTP 200を確認した。画面ロック中のため、作成ダイアログの実機操作確認は未実施。

- 削除確認をワークスペース名の再入力まで要求するUIへ変更し、本番ビルド後の画面表示を確認した。
- 更新失敗時の状態カード、詳細表示、自動再試行の進捗（`1 / 3`）を確認した。サービスが正常でも画面更新に失敗した場合は、保存済み内容を表示することが明示される。
- 診断情報コピーの導線をAIスタック状態ダイアログへ追加し、会話内容を含めずにランタイム状態とクライアント診断だけを扱う設計を確認した。
- チャットカードの部分要素欠落に対するイベント接続ガードを追加し、本番ビルド・完全再起動・APIヘルスチェック（HTTP 200）を確認した。
- 専用スモークワークスペースで設定を変更し、PATCH後の再取得で名前・共通指示・Agent選択ルールが保持されることを確認した（`settings-save=ok`）。確認名付き削除で後片付けした。
- 専用ワークスペースでAgent未設定の安全モードを確認し、Broadcastが `AGENT_SELECTION_REQUIRED`（400）で拒否された。その後、既定Agentと自動選択モードを設定するとBroadcastが202で受理された。確認名付き削除で後片付けした。
- 専用ワークスペースで存在しないAgent IDを既定Agentへ設定しようとしたところ、`AGENT_NOT_AVAILABLE`（400）で拒否された。無効Agentが保存状態へ入らないことを確認し、確認名付き削除で後片付けした。
- ローカルサーバーを既に使用中のポートへ二重起動し、従来のNode.jsスタックトレースを再現した。`caa0a80` で `EADDRINUSE` を利用者向けメッセージへ置き換え、同じ条件で「ポート4317は既に使用されています…」と終了することを確認した。既存プロセスやデータは変更していない。
- LibreChatの実Agentを指定したスモークを実行し、Nativeモードで通常生成が成功した。続けて同じ conversation ID の2ターン目を実行し、会話継続も成功した。MultiContextの保存データは変更していない。
- 最新本番ビルドを明示起動し、新規ワークスペースダイアログを実画面で確認した。説明文、名前欄、`0 / 120`、初期チャット数の1〜4件（2件おすすめ）、作成後のAgent設定案内が表示され、選択メニューの全選択肢とキャンセル復帰が正常だった。作成操作は行わず、保存データは変更していない。
- 最新本番ビルドのGUIから専用名 `UI検証用一時ワークスペース`、初期チャット数3件で作成した。作成成功トースト、一覧の `3件中3件のチャット`、各チャットのAgent未設定ガイダンスと送信無効状態を確認した。正確な確認名付き削除APIで後片付けし、再取得で同名0件・既存ワークスペース30件に戻った。
- 外部削除後に残った削除済みワークスペースIDを保持した状態で最新本番ビルドを再起動した。起動時に最新のアクティブワークスペースへ自動復帰し、`AGENT_MODE_SMOKE_1788710587656` の詳細画面が表示された。削除済みIDによる「ワークスペースが見つかりません」空画面を解消した。
- `default_agent_id` の保存不具合を、最新本番バンドルのサーバーを再起動して再検証した。作成APIの結果で `defaultAgentId` に指定Agent IDが残ることを確認した。検証中に旧サーバーが4317番ポートを占有していたため、バンドル更新時は旧プロセスを終了してから確認する運用上の注意も記録した。検証用ワークスペース3件は削除済み。
- Agent保存修正後の稼働サーバーで `AGENT_PERSIST_FINAL` を作成し、`defaultAgentId` が指定値と一致、初期チャット1件、削除204を確認した。再取得で検証用ワークスペース0件・全体30件となった。
- 最新稼働サーバーで `CORE_GENERATION_SMOKE` を作成し、Agent指定を保持したチャットへ正規の `.../members/:id/enqueue` で疎通プロンプトを送信した。HTTP 202、応答 `CORE_SMOKE_OK`、メッセージ2件、キュー0・idle復帰、削除204を確認した。検証途中に誤った `/send` URLを使った404は検証側のURL誤りであり、正規ルートでは成功した。
- CLIはリポジトリ直下の `data/state.json`、TauriはmacOSのアプリデータ領域に `state.json` を保存することを確認した。両者の状態件数が異なるのはこの意図された保存先分離による。検証用データを各サーバーへ混在させないよう、Tauri側に残っていた既知の検証名も再取得後に削除し、既存ワークスペース30件を維持した。
- P0 UX改善として、Agent未設定のアクティブチャットがある場合だけ送信前セットアップ案内を表示し、「設定を始める」で最初の未設定チャットのAgent選択へスクロール・フォーカスする導線を追加した。`node --check public/app.js`、`git diff --check`、サーバーバンドル生成、macOS本番ビルドを確認した。
- 製品画面をスクリーンショットで確認し、長いワークスペース名がヘッダー上で省略されても完全名を確認できるよう、編集欄のツールチップへ現在の名前を含めた。構文検査後、macOS本番ビルドを再実行する。
- BLOCKED相当のチャットエラー表示に「原因」と「次に選べる操作（再試行／停止）」を明示し、保持される履歴・キューの説明と分離した。画面の情報順を、状態 → 原因 → 保持データ → 操作に揃えた。
- 最新macOSバンドルで `DesktopApp E2E` を検索・選択し、Compile済み画面を実表示とAXツリーで確認した。`作成済み`、作成日時、`直近回答 2件を使用`、`結果をコピー`、`Markdownで保存` が表示され、メンバー履歴と統合レポートが分離されていた。検索後も既存ワークスペース件数30件を維持した。
- Tauri稼働サーバーで `PRODUCT_UX_BROADCAST_SMOKE` を作成し、2チャットへ同一プロンプトをBroadcastした。HTTP 202、両チャットが1秒で `SETTLED`／`idle`、各メッセージ2件、両回答が `PRODUCT_BROADCAST_OK` で一致することを確認した。確認名付き削除204で後片付けした。
- Tauri稼働サーバーで `PRODUCT_UX_COMPILE_SMOKE` を作成し、2チャットのBroadcast完了後にCompileを実行した。Broadcast後の各履歴2件がCompile前後で変わらず、CompileはHTTP 200・`lastCompile`生成で完了した。確認名付き削除204で後片付けした。
- Compile結果の長文履歴はスクリーンリーダーが更新のたびに全文を再読上げしないよう、結果領域の `aria-live` を外した。作成完了の通知は既存のトーストライブ領域で行う方針とし、画面の静的閲覧性を優先した。
- 要対応の `Navier-Stokes 5ペルソナ` を実画面で開き、チャット側にキュー13件がある一方、オーケストレーター側キューが空である状態を確認した。責務の違いを明示するため、実行管理バーに `チャット待機` 件数を追加し、一時停止ボタンの説明を「オーケストレーター専用」と実際の停止操作に合わせて修正した。
- 修正後の最新macOSバンドルを再起動し、同じ要対応ワークスペースで `チャット待機 13` と「チャットの待機キューは『全て停止』で停止できます」の説明をAXツリーで確認した。5件のチャットそれぞれに「原因」「履歴とキュー保持」「履歴整理」「整理後に再試行」が表示された。
- 要対応画面の集約ボタン `履歴整理を開く` を実クリックした。画面崩れやデータ変更はなく、対象チャット領域が維持された。macOS AX結果ではフォーカスがHTMLルートとして報告され、ブラウザ内部のフォーカス移動はこの環境のAX取得結果だけでは判定できなかったため、追加のアクセシビリティ実機確認事項として分類した。
- 最新macOSバンドルで新規ワークスペースダイアログを再確認した。用途説明、名前の入力例、`0 / 120`、初期チャット数（2件おすすめ）、作成後のAgent設定案内が表示され、キャンセル後に元の画面へ復帰した。作成・保存データの変更は行っていない。
- BLOCKEDチャットのエラー案内を状態に合わせて修正した。停止済みのエラーでは「再試行できます」、実行中のエラーだけが「実行を停止するか、再試行できます」と表示されるようにした。構文検査、差分検査、サーバーバンドル生成、macOS本番ビルドを確認した。
- 最新macOSバンドルのGUIで専用名 `製品導線スモーク 2026-09-07`、初期チャット2件のワークスペースを作成した。作成成功トースト、31件への増加、Agent未設定時の送信無効、Broadcast/Compileの安全な無効化、「設定を始める」導線を確認した。確認名付きDELETE 204で後片付けし、既存データは保持した。
- 外部削除後の自動復帰で、選択先は別ワークスペースへ切り替わる一方、周期更新が古いワークスペースの入力ドラフトを復元する表示不整合を実機で再現した。`tick()` にワークスペースID一致ガードを追加し、異なるワークスペースへ復帰した場合は旧ドラフトを復元しないよう修正した。構文検査、差分検査、macOS本番ビルドを確認した。
- 修正後の最新macOSバンドルを完全再起動し、アプリ本体が保存済みの復帰先ワークスペース名・指示・Agent設定を一致して表示することを確認した。削除済みワークスペースの名前や「未保存の変更」は残らず、既存30件を維持した。
- 稼働中の製品APIで専用ワークスペースを作成し、利用可能なAgentを既定値として設定した。2チャットへのBroadcastはHTTP 202で受理され、両チャットが`SETTLED`／`idle`へ復帰し、各メッセージ2件と`READY`応答を確認した。確認名付きDELETE 204で後片付けした。
- 稼働中の製品APIで2チャットへ回答を生成した後、Compileを実行した。CompileはHTTP 200で完了し、統合レポートが保存された。メンバー履歴は`[2,2]`件から`[2,2]`件のまま変わらず、Compileがチャット履歴を汚染しないことを確認した。確認名付きDELETE 204で後片付けした。
- 専用ワークスペースで2チャットへのBroadcast直後にStopを実行した。BroadcastはHTTP 202、StopはHTTP 200で、両チャットが`idle`・実行中なし・キュー0へ戻り、生成途中のメッセージも残らなかった。確認名付きDELETE 204で後片付けした。
- Agent未設定の専用ワークスペースで個別送信を試行した。複数Agent環境ではHTTP 400／`AGENT_SELECTION_REQUIRED`となり、エラーをキューや会話履歴へ混入させず、ワークスペースは`SETTLED`・`idle`のまま維持された。確認名付きDELETE 204で後片付けした。
- 最新フロント変更後にLibreChat実Agentを指定したNativeスモークを実行した。Agent 2体の検出、通常生成、同一conversation IDでの会話継続が成功した（`npm run smoke`）。MultiContextの保存データは変更していない。
- 専用ワークスペースを初期チャット4件で作成し、4チャットへ同時Broadcastした。HTTP 202で受理され、4件すべてが`idle`／`SETTLED`、4件すべてが回答保持、待機キュー0となった。確認名付きDELETE 204で後片付けした。
- 専用ワークスペースを初期チャット8件で作成し、8チャットへ同時Broadcastした。HTTP 202で受理され、8件すべてが`idle`／`SETTLED`、8件すべてが回答保持、待機キュー0となった。確認名付きDELETE 204で後片付けした。
- 8チャット並列の実動作確認に合わせ、新規作成UIの初期チャット数選択肢を1〜8件へ拡張した。入力値の上限も8件に統一し、構文検査とmacOS本番ビルドを確認した。
- 最新macOSバンドルを完全再起動し、新規作成ダイアログの選択メニューに1〜8件が表示されることをAX実機で確認した。メニューを閉じてキャンセルすると既存のワークスペース画面へ戻り、保存データは変更しなかった。
- 最新macOSバンドルで既存ワークスペースの名前欄を一時編集し、別ワークスペースへの移動を試行した。アプリ内ダイアログに遷移先に合わせた説明、`戻る`、`破棄して別のワークスペースへ移動` が表示された。`戻る`で編集状態に留まり、名前を元に戻すと`保存済み`へ復帰した。保存データは変更していない。
- 最新macOSバンドルで初期チャット数を`8件`へ変更し、補足説明が「多視点で回答を比較する構成」「実行時間とAgent使用量はチャット数に応じて増える」と切り替わることをAX実機で確認した。作成せずキャンセルし、既存データは変更していない。
- 8チャットでCompileまでの長時間フローを実行したところ、回答8件取得後もチャット内の追加キューが継続し、30秒以上`RUNNING`（最大4件実行中、待機キュー増減）となった。専用ワークスペースへStopを実行するとHTTP 200、実行中0・キュー0・`SETTLED`へ復帰し、その後DELETE 204で後片付けした。大規模なツール継続処理では、Compile前に収束条件・上限時間をUIで示すことを残課題とする。
- 実行管理バーへ`startedAt`由来の経過秒数表示を追加した。専用2チャット実行をGUIで検索・表示し、処理完了後に専用ワークスペースをDELETE 204で後片付けした。大規模なツール継続処理中の表示は次回、処理が継続しているタイミングで追加確認する。
- 経過秒数表示の初回実装がRESTサニタイズ処理で`startedAt`を落としていたため、公開用の`runningSince`を`store`と`application`の両経路へ追加した。最新バンドル再起動後、専用実行中ワークスペースで`inFlight=true`、`runningSince=true`、内部`current`非公開を確認し、Stop後DELETE 204で後片付けした。
- 経過秒数表示に加えて、30秒以上継続する実行を実行管理バー上で`長時間実行中`として強調表示するUIを追加した。`node --check`、`git diff --check`、macOS本番ビルドを確認した。8チャットのツール継続処理が収束しない場合は、表示を手掛かりに全停止へ移れる状態とし、収束条件・上限時間のUI提示は引き続き残課題とする。
- 実行管理バーの回答数表示を`回答済み`から`回答あり`へ変更した。過去の回答を含む観測値であることをツールチップと`aria-label`に明記し、未実行分の進捗と誤認しないようにした。最新macOSバンドルの画面確認はmacOSロック中のため未実施。構文検査、差分検査、macOS本番ビルド処理は実行済み。
- 接続断時にブラウザ標準の`Failed to fetch`をそのまま表示せず、「MultiContextに接続できないため状態確認と再試行が必要」と案内するよう共通`request()`を改善した。元の例外はセッション診断ログへ保存し、画面表示と診断情報を分離した。`NETWORK_UNAVAILABLE`コードを付与し、構文検査と差分検査を確認した。
- REST作成時の`memberCount`／`member_count`／`initialChatCount`を`initial_chat_count`の互換入力として受け付けるようにした。従来は作成自体が成功してもチャット0件になる入力があり、2件指定の一時ワークスペース作成で実際に2チャット生成、確認名付きDELETE 204まで確認した。
- 初期チャット数の不正値（例：`"many"`）を0件へ黙って変換せず、HTTP 400「初期チャット数は0〜10の整数で指定してください」を返すようにした。`memberCount: 2`で2チャット作成、異常値で400、正常系ワークスペースの確認名付きDELETE 204を一時サーバーで確認した。
- 複数の初期チャット数エイリアスが異なる値で同時指定された場合、先勝ちで作成せずHTTP 400「初期チャット数の指定が一致していません」を返すようにした。一時サーバーで`initial_chat_count: 2`と`memberCount: 4`の競合を確認した。
- 初期チャット数の型・範囲検証をワークスペース永続化より前へ移動した。真偽値などの不正値でHTTP 400となり、作成前後のワークスペース件数が`0 -> 0`で変化しないことを一時サーバーで確認した。
- 初期チャット数のエラーに`INVALID_CHAT_COUNT`（型・範囲）と`CHAT_COUNT_CONFLICT`（複数エイリアス競合）のコードを付与した。真偽値入力の実レスポンスでHTTP 400と`INVALID_CHAT_COUNT`を確認した。
- ワークスペース作成のJSON本体が`null`や配列の場合を永続化処理より前に拒否し、HTTP 400／`INVALID_REQUEST_BODY`を返すようにした。一時サーバーでJSON`null`を送信してこのレスポンスを確認した。
- ワークスペース更新（`PATCH`）でも`null`や配列のJSON本体をフィールド変換前に拒否し、HTTP 400／`INVALID_REQUEST_BODY`を返すようにした。作成・更新形状エラーの実レスポンスを確認し、正常系ワークスペースは確認名付きDELETE 204で後片付けした。
- 一斉送信の入力エラーに誤った「一時停止指定」ラベルが入っていた回帰を修正し、チャット個別送信にもJSON本体検証を追加した。構文検査、差分検査、`main`への反映を確認した。
- 最新`main`で実Agentを指定したNativeスモークを再実行した。LibreChatのAgent 2体検出、通常生成、同一conversation IDでの会話継続が成功した。
- Store層の404エラーに`WORKSPACE_NOT_FOUND`／`CHAT_NOT_FOUND`コードを付与し、存在しないワークスペースへの停止でHTTP 404と`WORKSPACE_NOT_FOUND`を実確認した。RESTエラー応答のコード欠落を修正した。
- 長時間のツール継続処理はコンテキスト抽出の正常な継続として扱い、MCPから停止ツールを削除した。MCPは生成・継続・抽出を担い、停止はGUIユーザー操作に限定する方針を`docs/MCP.md`とparity表へ反映した。
- MCPハンドラの`registerTool`を実装から数え、公開ツール20件・停止ツール0件を確認した。仕様書の件数誤記（17件）を20件へ修正した。
- macOSロック解除後、最新バンドルの実画面をAXツリーで確認した。AIスタックは`準備完了`、既存ワークスペース30件、Agent 2体、待機中ワークスペースの`全て停止`は無効、保存状態は`保存済み`だった。MCP停止ツール削除後のGUI表示も崩れていない。
- ソース変更後に古いmacOSバンドルが残っていたため、本番バンドルを再生成して完全再起動した。AXツリーで実行管理バーが旧`1/1`ではなく`回答あり 1/1`となり、過去の回答を含む説明付きツールチップも表示されることを確認した。サーバーバンドル、`.app`、`.dmg`の生成も成功した。
- Compile結果にスナップショット時刻、対象チャット数、対象メッセージ数を保存し、結果画面で「何を元に作ったレポートか」を表示するようにした。旧Compile結果は既存の`at`と履歴から後方互換表示し、`node --check`とmacOS本番ビルドを確認した。
- Compile表示変更後にmacOS本番バンドルを完全再起動し、実画面のAXツリーで`回答あり 1/1`、保存済み状態、AIスタック`準備完了`、停止ボタンの待機時無効を再確認した。既存ワークスペース30件は維持された。
- 稼働中のmacOSアプリAPIで専用2チャットワークスペースを作成し、Native生成完了後にCompileを実行した。`snapshotAt`、`sourceMemberCount: 2`、`sourceMessageCount: 4`、メンバー履歴`[2,2]`を確認し、確認名付きDELETE 204で後片付けした。

## 未確認

- Macの画面ロック中に実施できなかった、設定保存、再試行、メニュー開閉、ログ導線の通し操作。
