{
  "name": "multicontext-chat",
  "version": "0.2.0",
  "private": false,
  "type": "module",
  "description": "Parallel isolated LLM chats with queued cross-chat communication and optional response compression",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/scheduler.js && node --check src/store.js && node --check src/librechat.js && node --check src/openapi.js && node --check public/app.js && node --check public/desktop-ui.js && node --check public/runtimeLabels.js && node --check scripts/patch-librechat.mjs && node --check scripts/smoke.mjs && node --test",
    "build:server": "node scripts/bundle-server.mjs",
    "patch:librechat": "node scripts/patch-librechat.mjs",
    "smoke": "node --env-file-if-exists=.env scripts/smoke.mjs",
    "e2e:real": "node --env-file-if-exists=.env scripts/e2e-real.mjs",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "desktop:check": "cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "esbuild": "^0.25.12"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/node": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.5.4"
  }
}
