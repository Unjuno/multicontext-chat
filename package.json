{
  "name": "multicontext-chat",
  "version": "0.2.0",
  "private": false,
  "type": "module",
  "description": "Parallel isolated LLM chats with queued cross-chat communication and optional response compression",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/scheduler.js && node --check src/store.js && node --check src/librechat.js && node --check src/openapi.js && node --check public/app.js && node --check scripts/patch-librechat.mjs && node --check scripts/smoke.mjs && node --test",
    "patch:librechat": "node scripts/patch-librechat.mjs",
    "smoke": "node --env-file-if-exists=.env scripts/smoke.mjs"
  }
}
